# Crop-Disease-Prediction
This project focuses on detecting diseases in bell pepper (capsicum) leaves predicting model built with TensorFlow and (CNNs). It uses a focused dataset containing only healthy and bacterial spot-infected bell pepper leaves from the Plant Village dataset. A pre-trained sample model (model.h5) is included, along with a Python prediction.
